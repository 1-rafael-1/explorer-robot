[package]
name = "explorer-robot"
version = "0.1.0"
edition = "2024"
license = "MIT"
authors = ["Rafael Koch <rafael.koch@gmx.net>"]
description = "Explorer robot v3 — tracked robot with LiDAR-based autonomous navigation and sensor-driven obstacle avoidance, written in Rust with Embassy."
repository = "https://github.com/1-rafael-1/explorer-robot"

[workspace]
members = ["st7789-async"]
resolver = "3"

[features]
# Gate high-frequency diagnostic logging.
# Keep this OFF by default so release runs without a log consumer don't spend time formatting/queuing logs.
telemetry_logs = []

[lints]
workspace = true

[profile.dev]
lto = true
codegen-units = 1
debug = 2
debug-assertions = true
incremental = true
opt-level = 0

[profile.release]
codegen-units = 1
debug = 2
debug-assertions = false
incremental = true
lto = 'fat'
opt-level = 's'

[workspace.lints.clippy]
all = { level = "warn", priority = 1 }
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
cargo = { level = "warn", priority = -1 }
perf = { level = "warn", priority = -1 }
unwrap_used = "deny"
expect_used = "deny"
panic = "warn"
missing_docs_in_private_items = "warn"
cargo_common_metadata = "allow"
future_not_send = "allow"
multiple_crate_versions = "allow"

[dependencies]
embassy-rp = { version = "0.10.0", features = [
    "defmt",
    "unstable-pac",
    "time-driver",
    "critical-section-impl",
    "rp235xa",
] }
embassy-embedded-hal = { version = "0.6.0", features = ["defmt"] }
embassy-sync = { version = "0.8.0", features = ["defmt"] }
embassy-executor = { version = "0.10.0", features = [
    "platform-cortex-m",
    "executor-thread",
    "executor-interrupt",
    "defmt",
] }
embassy-time = { version = "0.5.0", features = [
    "defmt",
    "defmt-timestamp-uptime",
] }
embassy-futures = { version = "0.1.2" }
defmt = "1.0.1"
defmt-rtt = "1.1.0"
cortex-m-rt = "0.7.5"
critical-section = "1.2.0"
panic-probe = { version = "1.0.0", features = ["print-defmt"] }
nanorand = { version = "0.8.0", features = ["wyrand"], default-features = false }
ssd1306-async = { git = "https://github.com/kalkyl/ssd1306-async" }
embedded-graphics = "0.8.1"
static_cell = { version = "2.1.1", features = [] }
heapless = "0.9.2"
micromath = "2.1.0"
libm = "0.2"
icm20948-rs = { version = "0.2.3", features = ["async", "defmt", "dmp"] }
nalgebra = { version = "0.34", default-features = false, features = ["libm"] }
embedded-hal-async = "1.0.0"
sequential-storage = "7.1.0"
embedded-storage-async = "0.4.1"

# [patch.crates-io]
# embassy-rp = { git = "https://github.com/embassy-rs/embassy" }
# embassy-embedded-hal = { git = "https://github.com/embassy-rs/embassy" }
# embassy-sync = { git = "https://github.com/embassy-rs/embassy" }
# embassy-executor = { git = "https://github.com/embassy-rs/embassy" }
# embassy-time = { git = "https://github.com/embassy-rs/embassy" }
# embassy-futures = { git = "https://github.com/embassy-rs/embassy" }
